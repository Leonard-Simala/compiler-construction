void main ( ) {
    int a, b, sum;
    float avg;
    char grade;

    a = 10;
    b = 20;
    sum = a + b;
    avg = sum / 2;

    if (a < b && sum > 25) {
        grade = 'A';;
    }
    else {
        grade = 'B';;
    }

    return a;
}

int add ( int x int y ) {
    int result;
    result = x + y;
    return result;
}